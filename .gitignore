backend/scripts/.banner_test_backup.json

# Environment files with secrets
.env
backend/.env
backend/.env.qa
frontend/.env.local

# Dependency directories
node_modules/
frontend/node_modules/
backend/venv/
.venv/

# Python generated files
__pycache__/
*.py[cod]

# Next.js / TypeScript build output
frontend/.next/
frontend/out/
frontend/tsconfig.tsbuildinfo

# Local databases and temporary test artifacts
*.db
*.sqlite
*.sqlite3
_test_audio.webm
_test_audio_meta.json
backend/_test_audio.webm
backend/_test_audio_meta.json
backend/_stt_sim_output.txt

# Dev server logs
frontend/*.log

# Playwright test results
frontend/test-results/

# Stray empty lockfile stub
/package-lock.json

# Next.js auto-generated
frontend/next-env.d.ts

# Kilo build-tool scratch files (not project source)
.dbinfo
.schema
.tables
BUILD_PROMPT.md
KILO_BUILD.md
KILO_BUILD_PLAN.md
NURSEAI_BUILD_PLAN.md

# Internal ops notes — not for the public repo
DEPLOYMENT_CHECKLIST.md

# Ad-hoc manual/Playwright test output (screenshots, network captures) —
# can contain live tokens/cookies, never belongs in the repo
.playwright-mcp/
network-login.txt
/[0-9][0-9]-*.png
.vercel

# Ad-hoc landing screenshots / diff dumps kept locally, not repo content
diffstat.txt
/landing-*.jpeg
/*.png
backend/dev-server.log
.impeccable/

# Git worktrees used for isolated feature-branch work
.worktrees/

# Root .env.local — real secret leak gap found in 2026-09-06 audit
/.env.local

# Root-level QA run logs
/*.log

# Git Bash crash dumps on Windows
bash.exe.stackdump

# Orphaned frontend type-check artifact — no generator found in repo
frontend/.next-verify/

# Backend QA one-off run artifacts
backend/qa-artifacts/
backend/qa_*.log
backend/qa_*.txt

# Ad-hoc marketing brochure screenshots (local capture, not consumed by the app)
/speakoet-brochure-screenshots/
